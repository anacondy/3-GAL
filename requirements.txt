# 3-GAL - Galgotias University Examination Announcements Scraper
# Core Dependencies
#
# Phase 2 note: googletrans==4.0.0-rc1 has been REMOVED. The 4-year-old
# pre-release uses an undocumented Google Translate web endpoint that Google
# has been changing/banning. In practice it returns empty strings or raises
# AttributeError on any non-trivial Hindi text. Hindi PDFs are still detected
# by langdetect, but the English translation step is no longer attempted.
# (If you want translation later, use the official Google Cloud Translate API
# or a maintained alternative like deep-translator>=1.11.4.)

# Web Framework
Flask>=2.3.0

# Rate limiting (Phase 1)
flask-limiter>=3.5.0

# Web Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# PDF Processing
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Language Detection (translation removed — see note above)
langdetect>=1.0.9

# Production Server
gunicorn>=21.0.0
