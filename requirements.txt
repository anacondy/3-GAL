# 3-GAL - Galgotias University Examination Announcements Scraper
# Core Dependencies

# Web Framework
Flask>=2.3.0

# Rate limiting (Phase 1)
flask-limiter>=3.5.0

# Web Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# PDF Processing
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Language Detection (translation removed in Phase 2)
langdetect>=1.0.9

# Production Server
gunicorn>=21.0.0

# Phase 4: OCR fallback for image-only PDFs (OPTIONAL)
# To enable, install Tesseract OCR binary separately:
#   - Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   - Linux:   apt install tesseract-ocr
#   - macOS:   brew install tesseract
# Then set OCR_ENABLED=1 in your environment.
pdf2image>=1.16.0
pytesseract>=0.3.10

# Phase 4: CI test suite
pytest>=7.0.0
